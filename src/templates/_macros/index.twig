{% macro icon(name) %}
	{% set config = name is iterable ? name : {
		icon: name
	} %}
	{% include "upload/_macros/_icon" with (config) only %}
{% endmacro %}

{% macro preview(config) %}
	{% include "upload/_macros/_preview" with (config ?? {}) only %}
{% endmacro %}
